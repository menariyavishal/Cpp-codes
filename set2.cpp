#include<iostream>

using namespace std;

int main(){
    // int x, y;
    // cout<<"value of x is: "<<endl;
    // cin>>x;
    // cout<<"value of y is: "<<endl;
    // cin>>y;
    // cout<<"the sum is: "<<x+y;

    // Arithmatic operators
    int a, b;
    cout<<"Enter the value of a:"<<endl;
    cin>>a;
    cout<<"Enter the value of b:"<<endl;
    cin>>b;

    // cout<<"the outcome of a + b is: "<<a+b<<endl;
    // cout<<"the outcome of a - b is: "<<a-b<<endl;
    // cout<<"the outcome of a * b is: "<<a*b<<endl;
    // cout<<"the outcome of a / b is: "<<a/b<<endl;
    // cout<<"the outcome of a % b is: "<<a%b<<endl;
    // cout<<"the outcome of a++ is: "<<a++<<endl;
    // cout<<"the outcome of a-- is: "<<a--<<endl;
    // cout<<"the outcome of ++a is: "<<++a<<endl;
    // cout<<"the outcome of --a is: "<<--a<<endl;
    // cout<<"the outcome of b++ is: "<<b++<<endl;
    // cout<<"the outcome of b-- is: "<<b--<<endl;
    // cout<<"the outcome of ++b is: "<<++b<<endl;
    // cout<<"the outcome of --b is: "<<--b<<endl;

    // Comparision operators
   

    // cout<<"the outcome of a==b is: "<<(a==b)<<endl;
    // cout<<"the outcome of a!=b is: "<<(a!=b)<<endl;
    // cout<<"the outcome of a>=b is: "<<(a>=b)<<endl;
    // cout<<"the outcome of a<=b is: "<<(a<=b)<<endl;
    // cout<<"the outcome of a<b is: "<<(a<b)<<endl;
    // cout<<"the outcome of a>b is: "<<(a>b)<<endl;

    // Logical Operators:

    // cout<<"the outcome of logical AND OR operator is: "<<((a==b) || (a>=b) && (a>b))<<endl;
    // cout<<"the outcome of logical AND operator is: "<<((a==b) && (a<=b))<<endl;
    // cout<<"the outcome of logical OR operator is: "<<((a==b) || (a<=b))<<endl;
    // cout<<"the outcome of logical OR operator is: "<<((a==b) || (a>=b))<<endl;
    // cout<<"the outcome of logical NOT operator is: "<<(!(a==b))<<endl;
    // cout<<"the outcome of logical NOT AND operator is: "<<(!(a==b) && (a>=b))<<endl;
    // cout<<"the outcome of logical NOT OR  operator is: "<<(!(a==b) || (a>=b))<<endl;
    return 0;
}